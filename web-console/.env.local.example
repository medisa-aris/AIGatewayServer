# Upstream central-server REST API (server-side only — NOT exposed to the browser).
# The BFF (Next.js Route Handlers) proxies all /api/* traffic to this URL.
CENTRAL_SERVER_URL=http://localhost:10000
