# ---- Go (central-server) ----
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace file
go.work
go.work.sum

/central-server/server
/central-server/bin/

# ---- Node / Next.js (web-console) ----
node_modules/
.next/
out/
build/
*.tsbuildinfo

# ---- Environment files (keep *.example) ----
.env
.env.local
.env*.local

# ---- Claude local-only config ----
.claude/settings.local.json
# Local launch config — contains real DB credentials (use launch.json.example)
/.claude/launch.json

# ---- Logs ----
*.log
npm-debug.log*

# ---- OS / editor ----
.DS_Store
Thumbs.db
# .idea/
# .vscode/
